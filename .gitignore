# ── 민감 정보 (절대 커밋 금지!) ──
.email_config.json
.env
*.secret
# Firebase Admin SDK JSON 키 (전체 DB 접근 권한)
*firebase-adminsdk*.json
*firebase_adminsdk*.json
# Google 서비스 계정 키
service_account*.json
credentials*.json
# 유저 계정 + 비밀번호 해시
users.json

# ── 클라이언트 데이터 (개인정보 포함) ──
.clients/
.last_report_data.csv

# ── Streamlit 배포 설정 ──
.streamlit/secrets.toml
# Firebase secrets를 TOML 형태로 변환해 저장한 로컬 파일 (절대 커밋 금지)
firebase_secrets.toml
*secrets.toml
# 시크릿 변환 스크립트 (내부에 키가 하드코딩될 수 있음)
convert_secrets.py

# ── 스케줄러 런타임 ──
.scheduler.pid
.scheduler.stop
.scheduler.log

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo

# ── OS ──
.DS_Store
Thumbs.db
desktop.ini

# ── 테스트 산출물 ──
_test_*

# Claude Code ���� ����
.claude/
